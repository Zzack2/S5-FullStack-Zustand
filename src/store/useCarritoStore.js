import { create } from "zustand";

export const useCarritoStore = create((set) => ({
  // ---------------------------------------------------------------
  // EJERCICIO 4 (Zustand): declara aquí el estado del store.
  //
  // Necesitas:
  //   1. Un arreglo 'items' que empiece vacío.
  //   2. Una acción 'agregarItem(item)' que agregue un producto SIN
  //      mutar el arreglo original (usa el operador spread dentro
  //      de set(), igual que hiciste con setCarrito en App.jsx).
  //
  // Pista de forma (completa con tu propio código):
  //   items: [],
  //   agregarItem: (item) => set((state) => ({ ... })),
  // ---------------------------------------------------------------
}));
